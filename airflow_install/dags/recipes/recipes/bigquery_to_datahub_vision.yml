---
source:
  type: bigquery
  config:
    project_id: "vision-321214"
    options:
      credentials_path: "./vision_b.json"
    table_pattern:
      allow:
      - ".*"
      deny:
      - ".*_\$"

sink:
  type: "datahub-rest"
  config:
    server: 'http://localhost:8080'
