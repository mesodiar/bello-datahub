source:
  type: mysql
  config:
    # Coordinates
    host_port: mysql:3306
    database: datahub

    # Credentials
    username: datahub
    password: datahub
    
sink:
  type: "datahub-rest"
  config:
    server: 'datahub-gms:8080'